import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const PurchaseDome = () => {
    return(
        <SafeAreaView style={{ justifyContent:'center',alignItems:'center' }}>
          <Text style={{ fontSize:30,fontWeight:'bold' }}>Purchase Dome Screen</Text>
        </SafeAreaView>
    )
}

export default PurchaseDome;